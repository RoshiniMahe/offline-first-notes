{
  "name": "offline-first-api",
  "version": "1.0.0",
  "description": "Mock API for Offline First Notes App",
  "scripts": {
    "start": "json-server --watch db.json --port 3000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
